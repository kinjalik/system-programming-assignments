#include <gtest/gtest.h>
#include <string>
#include "parser.h"

using std::string;

TEST(ParserTest, BasicAssertions) {
    string input = "echo \"TEST TEST TEST\"";

//    CommandArray commands = parser(input.c_str());

//    ASSERT_NE(commands.tokens, nullptr);
//    ASSERT_EQ(commands.tokenCount, 1);
}